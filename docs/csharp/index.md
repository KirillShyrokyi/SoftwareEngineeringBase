# C# — язык и фундамент

> Этот раздел превращает синтаксис C# в рабочую модель языка: данные, значения, область видимости, управление потоком, методы, коллекции, LINQ, асинхронность и память.

## Что здесь будет

- Variables — переменные
- Values — значения
- Types — типы
- Scope — область видимости
- Methods & Parameters — методы и параметры
- Control Flow — управление выполнением
- Collections — коллекции
- LINQ
- Async / Await
- Memory Model — модель памяти

## Текущая тема

### Variables, Values, Parameters & Scope

Связь, которую мы должны построить:

```text
variable
   ↓
value
   ↓
type
   ↓
scope
   ↓
parameter
   ↓
copy / reference
   ↓
value type vs reference type
```

Полное объяснение будет добавляться после того, как тема разобрана в диалоге, проверена вопросами и сформулирована своими словами.

!!! note "Правило раздела"
    Мы не записываем сюда тему только потому, что она была прочитана. Сначала нужно доказать понимание ответами, примерами и вопросами.
